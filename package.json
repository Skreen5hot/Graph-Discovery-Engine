{
  "name": "graph-discovery-engine",
  "version": "0.0.0",
  "description": "GDE v2.1 — RPM Discovery-First engine and Query Builder UI for deterministic graph pattern expansion",
  "type": "module",
  "main": "dist/kernel/index.js",
  "types": "dist/kernel/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:tests": "tsc -p tsconfig.test.json",
    "start": "node dist/kernel/index.js",
    "test": "npm run build:tests && node dist-tests/tests/run-tests.js",
    "test:purity": "npm run build:tests && node dist-tests/scripts/ensure-kernel-purity.js",
    "verify": "npm run build && npm test && npm run test:purity",
    "demo": "node dist/demo.js",
    "dev:ui": "vite",
    "build:ui": "vite build",
    "build:demo": "VITE_STATIC_DEMO=true vite build",
    "clean": "rimraf dist dist-tests dist-ui"
  },
  "keywords": [
    "json-ld",
    "deterministic",
    "graph-discovery",
    "sparql",
    "knowledge-graph",
    "query-builder",
    "offline-first"
  ],
  "repository": {
    "type": "git",
    "url": ""
  },
  "homepage": "",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
